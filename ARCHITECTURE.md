# Configuration Architecture

## 📁 File Structure

```
office-management/
├── .env                 # ⚙️  Shared base configuration (committed to git)
├── .env.local          # 🏠 Local dev overrides (NOT committed)
├── .env.docker         # 🐳 Docker dev overrides (NOT committed)
├── docker-compose.yml  # 🐳 Uses .env + .env.docker
└── scripts/
    ├── dev-local.sh    # 🏃 Start local development
    ├── dev-docker.sh   # 🐳 Start Docker development
    └── dev-stop.sh     # 🛑 Stop all services
```

## 🔄 Configuration Flow

### Local Development

```
┌─────────────────────────────────────────────────────────────┐
│  1. npm run dev:local                                       │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Start Infrastructure (Docker)                           │
│     - PostgreSQL (port 5432)                               │
│     - Redis (port 6379)                                    │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Services Load Configuration                             │
│                                                             │
│     .env (base)      +      .env.local (overrides)         │
│     ─────────────           ───────────────────            │
│     NODE_ENV                MASTER_DATABASE_URL=           │
│     JWT_SECRET              postgres@localhost:5432        │
│     APP_NAME                                               │
│                             AUTH_SERVICE_URL=              │
│                             http://localhost:3001          │
│                                                             │
│     Result: Services connect to localhost                  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Start Services Locally (tsx watch)                     │
│                                                             │
│     Browser → localhost:3000 (Next.js)                    │
│              ↓                                             │
│              localhost:4000 (API Gateway)                  │
│              ↓                                             │
│              localhost:3001 (Auth Service)                 │
│              ↓                                             │
│              localhost:5432 (PostgreSQL in Docker)         │
└─────────────────────────────────────────────────────────────┘
```

### Docker Development

```
┌─────────────────────────────────────────────────────────────┐
│  1. npm run dev:docker                                      │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Docker Compose Loads Configuration                     │
│                                                             │
│     .env (base)      +      .env.docker (overrides)        │
│     ─────────────           ────────────────────           │
│     NODE_ENV                MASTER_DATABASE_URL=           │
│     JWT_SECRET              postgres@postgres:5432         │
│     APP_NAME                                               │
│                             AUTH_SERVICE_URL=              │
│                             http://auth-service:3001       │
│                                                             │
│     Result: Services use Docker service names              │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Build & Start All Containers                           │
│                                                             │
│     Browser → localhost:3000 (web container)              │
│              ↓                                             │
│              api-gateway:4000 (gateway container)          │
│              ↓                                             │
│              auth-service:3001 (auth container)            │
│              ↓                                             │
│              postgres:5432 (postgres container)            │
│                                                             │
│     Note: Docker internal DNS resolves service names       │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Key Insight

**Same Ports, Different Hostnames:**

| Environment | Database Connection | Auth Service URL |
|-------------|-------------------|------------------|
| **Local** | `postgresql://localhost:5432` | `http://localhost:3001` |
| **Docker** | `postgresql://postgres:5432` | `http://auth-service:3001` |

Port numbers stay the same (5432, 3001), only the **hostname** changes!

## 🔐 Configuration Priority

```
Higher Priority ────────────────────────> Lower Priority

Environment-Specific Override         Base Configuration
(.env.local or .env.docker)                (.env)
```

## 🌐 Network Topology

### Local Development
```
┌──────────────────────────────────────────────────────┐
│  Host Machine (macOS)                                │
│                                                      │
│  ┌─────────────┐   ┌──────────────┐                │
│  │ Next.js     │   │ API Gateway  │                │
│  │ :3000       │──>│ :4000        │                │
│  └─────────────┘   └──────────────┘                │
│                           │                          │
│                           ▼                          │
│  ┌─────────────┐   ┌──────────────┐                │
│  │ Auth Svc    │   │ Employee Svc │                │
│  │ :3001       │   │ :3002        │                │
│  └─────────────┘   └──────────────┘                │
│                           │                          │
│  ┌──────────────────────────────────────┐           │
│  │  Docker Network                      │           │
│  │  ┌─────────┐      ┌───────┐        │           │
│  │  │Postgres │      │ Redis │        │           │
│  │  │  :5432  │      │ :6379 │        │           │
│  │  └─────────┘      └───────┘        │           │
│  └──────────────────────────────────────┘           │
└──────────────────────────────────────────────────────┘
```

### Docker Development
```
┌──────────────────────────────────────────────────────┐
│  Docker Network (oms-network)                        │
│                                                      │
│  ┌─────────────┐                                    │
│  │    web      │  Container                         │
│  │   :3000     │                                    │
│  └──────┬──────┘                                    │
│         │                                            │
│         ▼                                            │
│  ┌─────────────┐                                    │
│  │ api-gateway │  Container                         │
│  │   :4000     │                                    │
│  └──────┬──────┘                                    │
│         │                                            │
│    ┌────┴─────┬─────────┬─────────┐                │
│    ▼          ▼         ▼         ▼                │
│  ┌────┐   ┌────┐   ┌────┐   ┌────┐                │
│  │auth│   │emp │   │att │   │prj │  Containers    │
│  │3001│   │3002│   │3003│   │3004│                │
│  └────┘   └────┘   └────┘   └────┘                │
│    │        │        │        │                     │
│    └────────┴────────┴────────┘                     │
│                │                                     │
│         ┌──────┴──────┐                            │
│         ▼             ▼                             │
│     ┌────────┐   ┌────────┐                        │
│     │postgres│   │ redis  │  Containers            │
│     │ :5432  │   │ :6379  │                        │
│     └────────┘   └────────┘                        │
│                                                      │
│  All services communicate via Docker DNS            │
│  (service name resolves to container IP)            │
└──────────────────────────────────────────────────────┘
                 │
                 │ Ports exposed to host
                 ▼
        Host: localhost:3000, :4000, etc.
```

## 🔍 How It Works

### Service Discovery

**Local Development:**
- Services use `localhost` + port number
- OS networking handles routing
- Direct process-to-process communication

**Docker Development:**
- Services use service name + port number
- Docker DNS resolves service name → container IP
- Docker network handles routing between containers

### Environment Variable Resolution

```javascript
// In your code (same in both environments):
const dbUrl = process.env.MASTER_DATABASE_URL;
const authUrl = process.env.AUTH_SERVICE_URL;

// Local Development:
// dbUrl = "postgresql://postgres:password@localhost:5432/oms_master"
// authUrl = "http://localhost:3001"

// Docker Development:
// dbUrl = "postgresql://postgres:password@postgres:5432/oms_master"
// authUrl = "http://auth-service:3001"
```

## 📊 Configuration Matrix

| Variable | .env (base) | .env.local | .env.docker |
|----------|-------------|------------|-------------|
| `NODE_ENV` | ✅ development | - | - |
| `JWT_SECRET` | ✅ shared-secret | - | - |
| `APP_NAME` | ✅ "OMS" | - | - |
| `MASTER_DATABASE_URL` | ❌ | ✅ localhost | ✅ postgres |
| `REDIS_URL` | ❌ | ✅ localhost | ✅ redis |
| `AUTH_SERVICE_URL` | ❌ | ✅ localhost | ✅ service name |
| `AWS_ENDPOINT` | ❌ | - | ✅ localstack |

✅ = Defined in this file  
❌ = Overridden in environment-specific file  
\- = Not needed in this environment

## 🎨 Visual Summary

```
╔═══════════════════════════════════════════════════════════╗
║              UNIFIED CONFIGURATION SYSTEM                  ║
╠═══════════════════════════════════════════════════════════╣
║                                                           ║
║  Same Code → Different Configs → Same Behavior           ║
║                                                           ║
║  ┌────────────┐      ┌────────────┐                     ║
║  │   Local    │      │   Docker   │                     ║
║  ├────────────┤      ├────────────┤                     ║
║  │ localhost  │      │  service   │                     ║
║  │  :3001     │      │   names    │                     ║
║  │  :5432     │      │  :3001     │                     ║
║  └────────────┘      │  :5432     │                     ║
║                      └────────────┘                     ║
║        │                    │                            ║
║        └────────┬───────────┘                            ║
║                 ▼                                        ║
║         ┌──────────────┐                                 ║
║         │  Same Ports  │                                 ║
║         │ Same Results │                                 ║
║         └──────────────┘                                 ║
╚═══════════════════════════════════════════════════════════╝
```

Perfect! ✨
