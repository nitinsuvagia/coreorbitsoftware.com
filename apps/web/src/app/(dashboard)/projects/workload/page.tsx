import { ComingSoon } from '@/components/ui/coming-soon';

export default function WorkloadPage() {
  return (
    <ComingSoon 
      title="Workload Management" 
      description="Visualize team workload and capacity. Balance work distribution across projects and team members."
    />
  );
}
